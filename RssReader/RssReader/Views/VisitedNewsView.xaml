<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:RssReader.Converters;assembly=RssReader"
             xmlns:controls="clr-namespace:RssReader.Controls;assembly=RssReader"
             x:Class="RssReader.Views.VisitedNewsView"
             Title="Visited">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Filter" Clicked="FilterMenuItem_OnClicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:VisitConverter x:Key="VisibilityConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout>
        <ListView x:Name="MyListView"
                  IsEnabled="{Binding IsEnabled}"
                  ItemsSource="{Binding News}"
                  ItemTapped="Handle_ItemTapped"
                  IsPullToRefreshEnabled="True"
                  RefreshCommand="{Binding Refresh}"
                  IsRefreshing="{Binding IsRefreshing}"
                  HasUnevenRows="True">

            <!--Built in Cells-->
            <ListView.ItemTemplate>
                <DataTemplate>
                    <controls:NewsViewCell News="{Binding .}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
